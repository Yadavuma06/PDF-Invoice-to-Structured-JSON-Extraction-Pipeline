# PDF-Invoice-to-Structured-JSON-Extraction-Pipeline
This project implements a rule-based extraction pipeline to convert unstructured invoice PDFs into structured JSON format. It handles non-uniform table layouts and performs validation between calculated totals and invoice totals.
